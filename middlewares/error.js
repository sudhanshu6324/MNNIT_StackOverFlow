const winston = require('winston');

module.exports = function (err,req,res,next){
    //winston.error = (err.message,err);

    //logging levels
    //error
    //warn
    //info
    //debug
    //silly
    

   // res.status(500).send("INternal server breakdown Somethng failed.");
};