
    var j=0;
$('.timestamp').each(function(i, obj) {
    var ds = $(this).html();
    date = moment(new Date(ds.substr(0, 16)));
console.log(date.format("DD-MMM-YYYY"));
    $(this).html(date.format("DD-MMM-YYYY"));
})
$('.content').each(function(i, obj) {
    var ds = $(this).html();        
    // $(this).html(unescape(ds));
    console.log(ds);
})
